<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccty.noah.mapper.UserMapper">

    <select id="queryInfoByUserName" resultType="java.lang.Integer">
       select 1
       from school_user
       where is_deleted = 0 and name = #{username}
       limit 1
    </select>

    <select id="queryUserInfoByUserName" resultType="com.ccty.noah.domain.database.UserDO">
        select id,name,nickname,sex,phone,language,country,city,area
        from school_user
        where is_deleted = 0 and name = #{name}
        limit 1
    </select>

    <select id="queryInfoByUserNameAndPwd" resultType="com.ccty.noah.domain.database.UserDO">
        select id,name,nickname,sex,phone,language,country,city,area
        from school_user
        where is_deleted = 0 and name = #{name} and password = #{password}
        limit 1
    </select>

    <select id="queryUserListByCondition" resultType="com.ccty.noah.domain.database.UserDO">
        select id,name,nickname,sex,level,phone,language,country,city,area
        from school_user
        where is_deleted = 0
        <if test="name!=null and name != ''">
            and name = #{name}
        </if>
        <if test="type!=null">
            and type = #{type}
        </if>
    </select>

    <insert id="insertUser" parameterType="com.ccty.noah.domain.database.UserDO">
        insert into school_user
        (type,manager_type,password,name,sex,phone)
        values
        (#{type},#{managerType},#{password},#{name},#{sex},#{phone})
    </insert>


</mapper>